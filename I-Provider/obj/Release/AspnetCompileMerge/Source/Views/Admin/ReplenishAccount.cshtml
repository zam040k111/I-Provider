@model I_Provider.Models.ReplenishAdminViewModel
@{
    ViewBag.Title = "Пополнение счета пользователя";
}
<h2>@ViewBag.Title</h2>
<hr />
@using (Html.BeginForm("ReplenishAccount", "Admin", "", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        @Html.LabelFor(n => n.Amount, "Сумма пополнения", new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(n => n.Amount, new { @class = "form-control", type = "number", min = "10", required = "true", placeholder = "Amount" })
            @Html.ValidationMessageFor(n => n.Amount, "", new { @class = "text-danger" })
        </div>
    </div>
    <input name="id" value="@ViewBag.Id" id="id" type="number" hidden />
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Пополнить" class="btn btn-default" />
        </div>
    </div>
}

